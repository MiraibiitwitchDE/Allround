name: Allround
version: '${version}'
main: net.mira.allround.Allround
api-version: 1.19
authors: [ Miraibii ]
description: Plugin that makes fun fun fun

commands:
  home:
    usage: use one of the following commands /home <set> <del> <tp>
